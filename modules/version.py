import sys
def check():
    version=sys.version
    if version>=sys.version:
        print("Yo've installed the corrected version")
        
        
    else:
        os.system("sudo apt install python3")
        os.system("sudo apt install pyton3-pip")
        os.system("sudo apt install python-is-python3")


